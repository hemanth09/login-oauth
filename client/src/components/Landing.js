import React from 'react';

const Landing = () => {
    return (
        <div style={{textAlign: "center"}}>
            <h1>
                Login!
            </h1>
            Login using Google oAuth flow
        </div>
    );
}

export default Landing;
